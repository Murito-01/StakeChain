{
  "name": "stakechain",
  "version": "1.0.0",
  "description": "MiniStake ERC-20 Token and Staking Protocol",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "seed": "hardhat run scripts/seed.js --network localhost",
    "dev": "vite"
  },
  "keywords": [
    "solidity",
    "staking",
    "hardhat",
    "erc20"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.4.2",
    "hardhat": "^2.22.3",
    "vite": "^5.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
